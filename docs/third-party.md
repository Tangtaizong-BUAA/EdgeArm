# Third-party attribution

- SO-101 MJCF, URDF and mesh assets: [TheRobotStudio/SO-ARM100](https://github.com/TheRobotStudio/SO-ARM100), Apache-2.0 as supplied by the upstream checkout. Existing README and citation retained.
- Motor-property attribution: Open Duck Mini, retained in the SO-101 README.
- Runtime packages (PyTorch, MuJoCo, NumPy, SciPy, Pillow, h5py, imageio, PyYAML) are installed as dependencies, not relicensed by this repository.
- Optional LeRobot exporter integration depends on LeRobot; no vendored LeRobot source is included.
- ACT is acknowledged as the historical algorithmic starting point; the final released actor is the staged modular policy documented here, not a repackaging of an official ACT model.

The Apache-2.0 license applies to this code release and the separately licensed
published checkpoints. It does not make the private training corpus public.
Preserve upstream notices when redistributing. Contributions must have compatible rights.

